name: bambi_signature_pad_widget_example
description: Example app for bambi_signature_pad_widget.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  bambi_signature_pad_widget:
    path: ../

flutter:
  uses-material-design: true
